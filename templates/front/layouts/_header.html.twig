{% block header %}
    <header class="masthead" style="background-image: url({{ bandeau }})">
        <div class="position-relative">
            <div class="row gx-4 gx-lg-5 justify-content-center">
                <div class="col-md-10 col-lg-8 col-xl-7">
                    <div class="site-heading">
                        <h1>{{ page_title }}</h1>
                        <h2 class="subheading">{{ subtitle_page }}</h2>
                        <span class="meta">{{ meta_title }}</span>
                    </div>
                </div>
            </div>
        </div>
    </header>
{% endblock %}
